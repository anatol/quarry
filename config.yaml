ignored_packages:
  - mini_racer
package_aliases:
  concurrent-ruby: ruby-concurrent  # 'concurrent-ruby' should be mapped to 'ruby-concurrent-ruby' package name but instead it named as 'ruby-concurrent'
  RedCloth: ruby-redcloth
  rubocop: rubocop
